#spring.profiles.active=@profiles.active@
  #应用名，需与创建应用时填的应用名对应（必填）-springboot配置
spring.application.name=sun-ai01
  #logging.config=classpath:log4j2-sit.xml
  #domain地址-edsp配置(必填项)
  #5.0.13-RELEASE版本之前：spring.boot.edsp.client.url = http://127.0.0.1:10010/edsp
edsp.cloud.client.domain-url=http://10.22.1.110:10010/edsp/
  #应用分组（即集群）-edsp配置(必填项，不填默认为default)
  #5.0.13-RELEASE版本之前：spring.boot.edsp.client.instance.group = default
edsp.cloud.instance.cluster=ai-cluster
  #实例代码，必须唯一-edsp配置 一般建议不配置，不配置会随机生成。
  #5.0.13-RELEASE版本之前：spring.boot.edsp.client.instance.code
  #5.0.13-RELEASE版本：edsp.cloud.instance.code
  #启动超时时间，默认300000毫秒，启动超过设置的时间，EDSP会判定实例启动异常，在实例列表界面状态会显示异常。
  #5.0.13-RELEASE版本之前：spring.boot.edsp.client.startTimeout = 300000
  #5.0.13-RELEASE版本：edsp.cloud.instance.startTimeout = 300000
  #实例code保存的路径，非必填项，非DEV环境下，程序启动会在${user.home}/.edsp/run/{appName}-{groupName}/下生成以code为文件名的文件
  #若指定了codeFile，则会在${spring.boot.edsp.client.instance.codeFile}/run/ 目录下生成以code为文件的文件名
  #spring.boot.edsp.client.instance.codeFile=P:\edsp
  #5.0.13-RELEASE版本已经去掉
  #分组token，PROD环境下token必填分组，并需要填写正确token，非PROD环境下非必填，但如果开启了token校验，则必须填写正确的token，否则注册不上
  #5.0.13-RELEASE版本之前：spring.boot.edsp.client.instance.token = 7aa02620-2e37-46fc-894b-8995843a5585
edsp.cloud.instance.token=157a4e38-e0f8-41df-b85b-591662ff0e1a
  #domain是否多实例，非必填项，默认值为false
  #edsp.cloud.client.multi-server = false
  #应用类型，非必填项；0：普通应用， 1：网关应用；默认为普通应用
  #edsp.cloud.instance.type = 0
  #edsp开关，必填项（不填默认为false）;false表示不使用edsp相关配置以及edsp资源组件；true表示使用对接edsp平台并使用edsp资源组件
  #5.0.13-RELEASE版本之前：spring.boot.edsp.client.enabled=true
edsp.cloud.enabled=true